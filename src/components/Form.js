import React, {Component} from "react";
import Weather from "./Weather";

class Form extends Component {

    render() {
        return (
            <div>
                Form
            </div>
        );
    }
}

export default Form;
